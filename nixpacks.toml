[phases.setup]
nixPkgs = ["python312", "ffmpeg", "libGL", "libGLU", "glib", "xorg.libxcb"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --workers 1 --threads 4 --timeout 300 --bind 0.0.0.0:$PORT"
